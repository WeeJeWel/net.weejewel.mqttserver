This app creates a MQTT Server (also called Broker), and publishes Homey Pro's devices' state to it.

It also supports the 'homie' convention (see https://homieiot.github.io) for compatibility with 3rd party MQTT clients.
